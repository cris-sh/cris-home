---
---
<script is:inline>
  // Simple redirect to default language or browser language
  const userLang = navigator.language || navigator.userLanguage;
  const targetLang = userLang.startsWith('es') ? 'es' : 'en';
  window.location.pathname = `/${targetLang}/`;
</script>
